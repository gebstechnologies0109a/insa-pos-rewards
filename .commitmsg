Fix HTTPS and analytics data loading

- Force HTTPS scheme in production via URL::forceScheme
- Trust all proxies for Forge Nginx SSL termination
- Add X-Requested-With header and credentials to fetch calls
- Add visible error banner when analytics API fails
- Check response status before parsing JSON
