Fix analytics: use database-agnostic DATE/HOUR functions for SQLite+MySQL

HOUR() and DATE() are MySQL-only. Added driver detection to use
strftime() on SQLite and native functions on MySQL. Also added
try-catch around stock_movements queries for resilience.
